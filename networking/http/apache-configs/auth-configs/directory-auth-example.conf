# 🔐 Directory Authentication Configuration
# Used in Apache Web Server Practical Lab - Exercise 5c
# This exact configuration was implemented and tested successfully

<Directory "/var/www/html/teste">
    AuthType Basic
    AuthName "Acesso Restrito ao Teste"
    AuthUserFile /etc/htpasswd/.htpasswd
    Require valid-user
</Directory>

# ============================================================
# IMPLEMENTATION DETAILS:
# ============================================================
# Location: /etc/apache2/sites-available/000-default.conf
# Purpose: Restrict access to /teste directory only
# Test URL: http://www.xpto.cb/teste/
# Valid Users: joao (xpto123), maria (yujg), miguel (mikesry)
# 
# VERIFICATION COMMANDS:
# ============================================================
# Test without credentials:
#   wget -q -O - http://www.xpto.cb/teste/
#   Expected: 401 Unauthorized (no output)
#
# Test with credentials:
#   wget -q -O - --user=joao --password=xpto123 http://www.xpto.cb/teste/
#   Expected: <h1>Página de Teste</h1>
#
# ============================================================
# WHY THIS APPROACH (vs .htaccess):
# ============================================================
# 1. ✅ Security: Configuration in protected /etc/apache2/ directory
# 2. ✅ Performance: Apache doesn't check for .htaccess on every request
# 3. ✅ Centralized: All configuration in one place
# 4. ✅ Best Practice: Recommended by Apache documentation
